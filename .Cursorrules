# 项目名称：电影剪辑团队专属文案美化工具

## 项目背景与用户画像
我是一个电影剪辑博主，我和一个团队一起工作。团队其他成员不懂编程，我们日常需要将“口语化的剪辑笔记/文案草稿”美化成“有爆款潜力的解说文案”。

**痛点：**
1. 通用 AI (如 ChatGPT) 写出的文案“AI味”太重。
2. 我懂一点基本逻辑(如使用 Cursor)，但无法写出完整代码。
3. 我需要一个 **非技术背景的团队成员也能轻松使用** 的图形化工具(网站/本地程序)。
4. 我希望这个工具能接入目前最好的文本处理 AI(云端大模型API)，或者未来能培养一个我们自己专属的本地 AI 模型。

## 具体要求与功能清单

### 1. 交付形式
- **类型**：一个 **单页面的 Web 应用程序**。
- **界面**：极简主义风格，类似记事本或备忘录。
- **核心区域**:
    - 上方是一个大的输入框(Textarea)，用于粘贴原始草稿。
    - 中间是一个醒目的 **“一键美化”** 按钮。
    - 下方是一个输出框，用于展示美化后的结果。
    - 最好有一个 **“复制结果”** 按钮。

### 2. 技术栈偏好(请 Cursor 协助选择与搭建)
- 由于我不懂代码部署，请使用 **纯 HTML + CSS + JavaScript** 编写，确保我只要双击 `index.html` 文件就能用浏览器打开使用。
- **样式**：背景深色模式(适合剪辑师熬夜写稿)，字体要大，排版舒服。

### 3. AI 核心功能配置(这是最关键的部分)
请帮我设计 **两种模式**，通过界面上的下拉菜单或标签页切换：

**模式 A:云端 API 模式(初期使用，效果好，无硬件门槛)**
- 接入 **智谱 AI (GLM-4-Flash)** 的免费 API 或 **DeepSeek API**。
- 程序需要包含一个简单的 **“设置”按钮**，允许用户填入自己的 API Key 并保存在浏览器本地(LocalStorage)，不上传服务器。
- **美化逻辑**:必须严格按照我在下方“AI 行为准则”中定义的文案美化规则进行调用。

**模式 B:本地 Ollama 模式(进阶使用，隐私安全)**
- 提供一个输入框，让用户填写本地 Ollama 服务的地址(例如 `http://localhost:11434`)。
- 提供一个下拉菜单，自动获取本地已下载的模型列表(如 `deepseek-r1:7b`, `qwen2.5:7b`)。
- **提示**：如果检测不到本地服务，请显示友好的文字提示：“请确保已在电脑上安装并运行 Ollama”。

### 4. AI 行为准则(非常重要，必须内嵌在代码中作为 System Prompt)
无论使用哪种模式，发给 AI 的 **System Prompt(系统提示词)** 必须严格遵循以下规则。这是该工具的核心灵魂，严禁偏离。

---
**角色设定：**
你是一位顶级的影视解说文案专家和语言风格美化师。你擅长将口语化、逻辑松散的电影剪辑博主笔记，优化为节奏感强、有感染力、逻辑清晰的专业解说文案。

**核心任务：**
你的任务是“美化”，而非“重写”。你必须严格遵守以下核心原则：
1. **尊重并保留原文内容**：绝对忠于用户提供的文案内容、观点、剧情顺序和核心信息。严禁添加原文中没有的剧情解读、人物分析或主观评价。
2. **专注语言表达**：你的所有工作仅限于优化原文的语言表达，使其更流畅、生动、有节奏感，符合优秀电影解说文案的标准。
3. **保留用户口吻**：在美化时，必须保留原文的第一人称或第二人称视角，以及用户独特的语气，如调侃、惊叹、吐槽等。

**语言风格指南(请在处理时遵循以下风格):**
1. **口语化与节奏感**：句子要短促有力，多断句。模仿自然说话的语气，多用反问、设问和感叹句来调动观众情绪。例如：“你猜怎么着？”“这你敢信？”
2. **关键词打磨**：仔细打磨文案中的动词和形容词，用更具画面感和冲击力的词汇替代平淡的描述。
   - 平淡：“他很难过” -> 美化后：“他彻底破防了” 或 “他整个人都碎了”
   - 平淡：“场面很大” -> 美化后：“经费在燃烧” 或 “史诗级的名场面”
3. **结构优化**：在保留所有剧情节点的前提下，合理使用“但是”、“然而”、“更绝的是”等连接词，增强文案的转折和悬念感。
4. **避免“AI味儿”**：严禁使用任何“首先、其次、最后、总而言之”这类刻板的连接词，以及“这部影片深刻揭示了……”等说教式总结。要让文案听起来像一个懂电影的博主在和你面对面聊天。

**输出格式：**
- 直接输出美化后的完整文案，无需任何前缀、后记或解释说明。
- 如果用户提供了多个版本的文案进行对比，请先简要说明各版本在表达上的差异，再输出你认为最优美化的版本。
---

### 5. 开发约束
- **代码注释**：因为是 AI 生成的代码，请 **务必添加详细的中文注释**，解释每一段逻辑是做什么的。这样我以后可以尝试对着代码学习修改。
- **错误处理**：如果 API 调用失败(比如网络不通、Key 失效)，不要崩溃，要在输出框里显示红色的、通俗易懂的错误原因(例如：“网络好像开小差了，检查一下 Key 对不对？”)。

## 最终目标
请在 Cursor 中帮我生成这个完整项目的所有文件。如果我需要修改样式或提示词，你后续可以指导我在哪个文件、哪一行进行修改。


# 项目升级需求：电影剪辑团队文案美化工具 V2.0

## ⚠️ 核心原则（必须严格遵守）

1. **保留所有现有内容**：本次升级是**增量开发**，不得删除或覆盖项目中任何已有的功能、样式或代码逻辑。
2. **删除前必须确认**：如果你认为某段旧代码与新功能冲突需要删除，**必须先向我说明原因并等待我确认**，不得擅自删除。
3. **只做添加和优化**：请在现有代码的基础上，新增以下功能模块，并与现有界面和谐融合。

---

## 一、项目背景与当前状态
我已经有一个由 Cursor 生成的本地 HTML/CSS/JS 文案美化工具，并且我已对其做了一些个性化的微调。现在需要将其升级为一个**线上可访问、支持本地模型调用、具备团队协作保存功能**的版本。

## 二、本次需要新增的功能模块

### 1. 线上部署支持（GitHub + Vercel）
- 在项目根目录生成一个 `README.md` 文件，内容需包含：
  - 如何将项目推送到 GitHub 仓库。
  - 如何通过 Vercel 关联 GitHub 仓库进行一键部署。
  - 如何配置环境变量(如果有需要的话)。
- 生成 `.gitignore` 文件，忽略 `node_modules`、`.env`、`.DS_Store` 等无关文件。

### 2. 模式 B 的本地配置指南（内嵌帮助弹窗）
- 在模式 B 区域(或附近)**新增**一个“❓ 使用帮助”按钮。
- 点击按钮后，**弹出模态框(Modal)**，显示以下帮助内容(请保持格式清晰):

---
**如何启用本地模型(模式 B)?**

1. **确保已安装 Ollama**
   - 前往 [ollama.com](https://ollama.com) 下载并安装。
   - 打开终端(CMD 或 PowerShell),运行 `ollama pull deepseek-r1:7b`(或你喜欢的模型)。

2. **启动 Ollama 并允许网页访问**
   - **Windows 用户**:
     1. 关闭当前运行的 Ollama(任务栏右键退出)。
     2. 以管理员身份打开 **命令提示符**。
     3. 输入以下命令并按回车：
        ```cmd
        set OLLAMA_ORIGINS=*
        ollama serve
        ```
     4. 保持此窗口**不要关闭**。
   - **macOS / Linux 用户**:
     1. 打开终端。
     2. 输入以下命令并按回车：
        ```bash
        export OLLAMA_ORIGINS=*
        ollama serve
        ```
     3. 保持此终端窗口不要关闭。

3. **在下方输入 Ollama 地址**
   - 默认地址为 `http://localhost:11434`。
   - 如果浏览器提示“不安全内容”，请点击地址栏右侧的盾牌图标，选择“加载不安全脚本”。

4. **点击“获取模型列表”** 选择你要使用的模型，即可开始美化。
---

- 弹窗应包含一个“关闭”按钮或点击遮罩层关闭。

### 3. 云空间功能（基于 Supabase 实现文案保存与团队共享）

#### 3.1 功能概述
- 使用 **Supabase** 作为后端服务。
- 实现**邮箱注册/登录**功能。
- 登录后，用户可以**保存当前文案**(原始草稿 + 美化结果)到云端。
- 提供**“我的文案库”**面板，展示当前用户保存的所有历史记录，支持点击查看、复制和删除。
- 提供**“团队共享区”**面板，展示其他成员公开分享的文案(可选功能，如实现复杂可先简化)。

#### 3.2 界面要求
- **新增登录/注册入口**：可在右上角添加“登录/注册”按钮，点击后弹出登录模态框。
- **新增保存按钮**：在美化结果输出框附近，添加“保存到云空间”按钮。
- **新增文案库面板**：可通过侧边栏抽屉或独立页面实现，请根据现有布局选择最不突兀的方式。
  - 每条记录显示:保存时间、原始文案前20字、美化文案前20字。
  - 每条记录旁提供“复制”、“删除”图标按钮。
  - 顶部提供“我的文案”和“团队共享”两个标签页切换。

#### 3.3 技术实现指引（请在代码注释中说明）
- 在 `script.js` 顶部预留 Supabase 配置占位符：
  ```javascript
  const SUPABASE_URL = 'YOUR_SUPABASE_URL'; // 请替换为你的项目URL
  const SUPABASE_ANON_KEY = 'YOUR_SUPABASE_ANON_KEY'; // 请替换为你的anon key